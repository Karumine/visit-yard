# Prompt สำหรับสร้างเว็บแอป "รายงานการเข้าเยี่ยมลูกค้า/Yard" (ใช้งานบน iPad)

> วิธีใช้: คัดลอกทั้งไฟล์นี้ไปวางเป็น prompt เดียว (Claude / Claude Code / v0 / Cursor) แล้วสั่งว่า "สร้างแอปตามสเปคนี้"
> ถ้าเครื่องมือจำกัดความยาว ให้ส่งส่วนที่ 1–4 ก่อน แล้วค่อยส่ง 5–11 เป็นรอบถัดไป

---

## 1. บทบาทและเป้าหมาย

คุณคือ senior frontend engineer ที่ถนัด PWA และงาน form-heavy บนแท็บเล็ต
สร้าง **เว็บแอปแบบหน้าเดียว (SPA) ที่ติดตั้งเป็น PWA ได้** สำหรับให้วิศวกร/เจ้าหน้าที่สินเชื่อกรอก "รายงานการเข้าเยี่ยม ลูกค้า/Yard" ขณะอยู่หน้างานจริง แทนการกรอกใน Excel

**เกณฑ์ความสำเร็จ:** วิศวกรเดินเข้าโรงงาน เปิดแอปบน iPad กรอกจนจบ ถ่ายรูปแนบ เซ็นชื่อ และกดออกเป็น PDF ได้ครบภายในรอบเดียว โดยไม่ต้องใช้อินเทอร์เน็ต และไม่ต้องกลับมาพิมพ์ซ้ำในออฟฟิศ

---

## 2. บริบทผู้ใช้ (สำคัญมากต่อการออกแบบ UX)

- อุปกรณ์หลัก: **iPad (Safari) แนวตั้งและแนวนอน** — ต้องรองรับทั้งสอง orientation
- สภาพแวดล้อม: โรงงาน/ลานเครื่องจักร แดดจ้า มือถือของ มีฝุ่น อาจใส่ถุงมือ
- สัญญาณเน็ต: **ไม่แน่นอน หรือไม่มีเลย** → ทุกอย่างต้องทำงาน offline ได้เต็มรูปแบบ
- ผู้ใช้ไม่ใช่สายไอที → ปุ่มใหญ่ ข้อความชัด ไม่มีศัพท์เทคนิค
- ภาษา: **ภาษาไทยทั้งหมด** (ยกเว้นชื่อฟิลด์ที่ฟอร์มเดิมใช้ภาษาอังกฤษ เช่น Yard, Srv. Team, Op. Team ให้คงไว้)
- วันที่ใช้ **พ.ศ.** รูปแบบ `dd-mm-yy` (เช่น 31-07-69 = 31 ก.ค. 2569) ตามฟอร์มเดิม

---

## 3. ข้อกำหนดทางเทคนิค

- **Stack:** React + TypeScript + Vite, Tailwind CSS, React Hook Form + Zod (validation), Zustand หรือ Context สำหรับ state
- **PWA:** manifest + service worker, `display: standalone`, ติดตั้งลง Home Screen ของ iPad ได้, ทำงาน offline 100%
- **Storage:** IndexedDB (แนะนำ Dexie.js) เก็บทั้ง draft, รายงานที่เสร็จแล้ว, และรูปภาพ (เก็บเป็น Blob ไม่ใช่ base64 ใน state)
- **Autosave:** บันทึกอัตโนมัติทุก 3 วินาทีหลังหยุดพิมพ์ + ทุกครั้งที่เปลี่ยนหน้า และแสดงข้อความ "บันทึกอัตโนมัติแล้ว HH:mm" ที่มุมบน — ห้ามข้อมูลหายเด็ดขาดถ้า Safari kill tab
- **ไม่มี backend ในเวอร์ชันแรก** แต่ให้แยกชั้น `storage.ts` เป็น interface ให้ต่อ API ภายหลังได้ทันที
- **ฟอนต์:** Noto Sans Thai หรือ Sarabun (embed ไว้ในแอป ห้ามโหลดจาก CDN ตอน runtime เพราะต้อง offline)
- **Touch target ขั้นต่ำ 48×48 px**, font-size อินพุตขั้นต่ำ 16px (กัน iOS zoom อัตโนมัติ)
- โค้ดต้องแยกไฟล์ชัดเจน: `types/`, `components/`, `screens/`, `lib/` (pdf, storage, image)

---

## 4. โครงสร้างข้อมูล (สร้าง TypeScript type ตามนี้ให้ครบทุกฟิลด์)

### 4.1 ส่วนหัว
| ฟิลด์ | ชนิด | หมายเหตุ |
|---|---|---|
| `visitDate` | date | วันที่เข้าเยี่ยม (พ.ศ.) ค่าเริ่มต้น = วันนี้ |
| `visitNo` | number | ครั้งที่เข้าเยี่ยม ค่าเริ่มต้น 1 |
| `customerType` | 'existing' \| 'new' | ลูกค้าเดิม / ลูกค้าใหม่ |
| `yardType` | 'existing' \| 'new' | Yard เดิม / Yard ใหม่ |
| `companyName` | text | ชื่อบริษัทที่เข้าเยี่ยม (รองรับหลายชื่อคั่น /) |
| `address` | textarea | ที่อยู่ |
| `assetGroupType` | 'existing' \| 'new' | เครื่องจักรกลุ่มเดิม / กลุ่มใหม่ |
| `machineType` | textarea | ประเภทเครื่องจักร |
| `visitorBusinessNature` | textarea | ลักษณะธุรกิจผู้เข้าเยี่ยม |

### 4.2 ผู้ที่ได้เข้าพบ (ลูกค้า/Yard) — array, เริ่มต้น 3 แถว, เพิ่ม/ลบได้
`contacts[]`: `{ name: string; phone: string; position: string }`
- ปุ่มโทรออกจากเบอร์ได้ (`tel:`) และปุ่ม "+ เพิ่มผู้ติดต่อ"

### 4.3 เนื้อหาการเยี่ยม
| ฟิลด์ | ชนิด |
|---|---|
| `visitPurpose` | textarea — วัตถุประสงค์ในการเยี่ยม |
| `visitDetail` | textarea (ยาว) — รายละเอียดการเข้าเยี่ยม |
| `financedMachineInfo` | textarea — ข้อมูลเครื่องจักรที่จะขอสินเชื่อ |
| `machinePhotos` | Photo[] — รูปประกอบเครื่องจักร |
| `customerBackground` | textarea (ยาว) — ข้อมูลลูกค้าเบื้องต้น |
| `sitePhotos` | Photo[] — รูปสถานประกอบการ/โกดัง/Workshop |

`Photo = { id, blob, thumbnailBlob, caption?, takenAt, gps?: {lat,lng} }`

### 4.4 ข้อสังเกต (8 หัวข้อ — ตามฟอร์มเดิมเป๊ะ)
1. **ประสบการณ์** — 4 ช่องย่อย: `เป็นผู้ปฏิบัติ`, `เป็นผู้บริหาร`, `ธุรกิจองค์กร`, `ธุรกิจครอบครัว`
2. **ทีมงาน Service/Operation** — ช่องหลัก + 2 ช่องย่อย: `Srv. Team`, `Op. Team`
3. **การจัดการอะไหล่** — textarea
4. **ระบบการป้องกันการสูญหายของเครื่องจักร** — textarea
5. **ลักษณะลูกค้าที่มี** — textarea
6. **มูลค่าของเครื่องจักร** (หน่วย: ล้านบาท) — `สูงสุด`, `ต่ำสุด`, `เฉลี่ย`, `ทั้งระบบ` + ช่องหมายเหตุ (ค่าเริ่มต้น "เป็นยอดขาย/ปี")
   - ให้คำนวณ `เฉลี่ย` อัตโนมัติจาก (สูงสุด+ต่ำสุด)/2 แต่ผู้ใช้แก้ทับได้
7. **คู่แข่ง** — textarea
8. **อื่นๆ (ถ้ามี)** — textarea

### 4.5 ความเห็น/ความน่าสนใจ — ให้คะแนน 0–10
`scores`: `businessNature`, `ownerCharacter`, `customerBase`, `partnership`
- แสดงป้ายกำกับ: **0 = น้อยที่สุด, 10 = มากที่สุด**
- คำนวณและแสดง **คะแนนเฉลี่ยรวม** แบบ real-time (ฟอร์มเดิมไม่มี แต่มีประโยชน์ — ใส่เป็นข้อมูลเสริม)

### 4.6 ปิดท้าย
| ฟิลด์ | ชนิด |
|---|---|
| `nextAction` | textarea — สรุปการดำเนินการต่อไป |
| `nextAppointment` | date (ว่างได้) — นัดครั้งถัดไป |
| `hasAttachment` | boolean — เอกสารแนบ มี/ไม่มี |
| `attachments` | File[] — ถ้าเลือก "มี" ให้แนบไฟล์/รูปได้ |
| `inspectorAA` | text + signature — ลงชื่อผู้เข้าเยี่ยม (AA) |

### 4.7 ช่องเซ็นอนุมัติ (Inspected by) — 3 ช่อง
`approvals`: `engineering`, `credit`, `generalManager`
แต่ละช่อง: `{ signature?: Blob; name?: string; date?: string }` — เว้นว่างได้ (ปกติเซ็นในออฟฟิศ) แต่ต้องมีพื้นที่ในไฟล์ PDF เสมอ

---

## 5. หน้าจอและ Flow

### 5.1 หน้าแรก — รายการรายงาน
- การ์ดรายงานแต่ละใบ: ชื่อบริษัท / วันที่ / สถานะ (`ร่าง` เหลือง, `เสร็จแล้ว` เขียว) / คะแนนเฉลี่ย
- ช่องค้นหาตามชื่อบริษัท + ตัวกรองสถานะ
- ปุ่มใหญ่ **"+ สร้างรายงานใหม่"**
- แต่ละการ์ดมีเมนู: เปิด / ทำสำเนา (duplicate) / ออก PDF / ลบ (ต้องยืนยัน)

### 5.2 หน้ากรอกรายงาน — แบบ Wizard 6 ขั้น
มีแถบ stepper ด้านบนแสดงขั้นและจุดเขียวเมื่อกรอกครบ กดข้ามไปขั้นใดก็ได้

1. **ข้อมูลทั่วไป** (4.1)
2. **ผู้ที่ได้เข้าพบ + วัตถุประสงค์** (4.2, visitPurpose)
3. **รายละเอียด + รูปภาพ** (4.3)
4. **ข้อสังเกต 8 ข้อ** (4.4) — ใช้ accordion แต่ละหัวข้อ กันหน้ายาวเกิน
5. **ให้คะแนน** (4.5)
6. **สรุป + ลายเซ็น** (4.6, 4.7) + ปุ่ม "ดูตัวอย่าง PDF" และ "บันทึกเป็นเสร็จสิ้น"

ปุ่ม **ย้อนกลับ / ถัดไป** ตรึงไว้ล่างจอ (safe-area ของ iPad) เสมอ

---

## 6. คอมโพเนนต์เฉพาะที่ต้องทำให้ดี

**PhotoCapture**
- ปุ่มเดียวเปิดกล้อง iPad ได้ทันที (`<input type="file" accept="image/*" capture="environment">`) + เลือกจากอัลบั้มได้
- ย่อรูปอัตโนมัติเหลือด้านยาวสุด 1600px, JPEG quality 0.75 ก่อนเก็บ (กันเปลือง storage)
- แสดงเป็น grid 3 คอลัมน์ (แนวนอน 4), แตะเพื่อดูเต็มจอ, ลากสลับลำดับได้, ใส่คำบรรยายใต้รูปได้
- แสดงจำนวนรูป และขนาดรวมที่ใช้

**ScoreSlider (0–10)**
- ปุ่มตัวเลข 0–10 เรียงเป็นแถว แตะเลือก (แม่นกว่า slider เมื่อใส่ถุงมือ) + สีไล่จากแดง→เหลือง→เขียว
- แสดงตัวเลขที่เลือกตัวใหญ่ทางขวา

**SignaturePad**
- วาดด้วยนิ้ว/Apple Pencil บน canvas, ปุ่ม "ล้าง", บันทึกเป็น PNG พื้นหลังโปร่งใส
- เปิดเป็น modal เต็มความกว้าง สูงอย่างน้อย 200px

**CurrencyInput** — คีย์บอร์ดตัวเลข, ใส่ comma อัตโนมัติ, ต่อท้ายด้วย "ล้านบาท"

**ThaiDatePicker** — แสดง/รับค่าเป็น พ.ศ. เก็บภายในเป็น ISO date

---

## 7. Validation

- ก่อนกด "บันทึกเป็นเสร็จสิ้น" ต้องมีอย่างน้อย: `visitDate`, `companyName`, ผู้ติดต่ออย่างน้อย 1 คน, `visitPurpose`, คะแนนครบ 4 ข้อ, และรูปอย่างน้อย 1 รูป
- ถ้ายังไม่ครบ ให้แสดงรายการ "ยังขาดข้อมูล" เป็น list พร้อมลิงก์กระโดดไปช่องนั้น — **อย่าบล็อกเงียบๆ**
- ทุกช่องกรอกไม่ครบยังบันทึกเป็น "ร่าง" ได้เสมอ

---

## 8. การส่งออก (Export)

**PDF (สำคัญที่สุด)** — ใช้ `pdfmake` หรือ `jsPDF + html2canvas` พร้อมฝังฟอนต์ไทย
- ขนาด A4 แนวตั้ง จัดวางให้ **ใกล้เคียงฟอร์ม Excel เดิม**: หัวเรื่อง "รายงานการเข้าเยี่ยม ลูกค้า/Yard" + โลโก้บริษัทมุมซ้ายบน, ตารางสองคอลัมน์ (ป้ายชื่อซ้าย / ค่าขวา), เส้นใต้แต่ละบรรทัด
- รูปภาพจัดเป็น grid ในตำแหน่งเดียวกับฟอร์มเดิม
- คะแนน 4 ข้อแสดงเป็นกล่องพื้นฟ้าอ่อนเหมือนต้นฉบับ
- ท้ายเอกสารมีช่องเซ็น 3 ช่อง: Engineering Department / Credit Department / General Manager พร้อมบรรทัด Date …../…../……
- ชื่อไฟล์: `รายงานเข้าเยี่ยม_[ชื่อบริษัท]_[วันที่].pdf`
- ต้องแชร์ผ่าน iOS Share Sheet ได้ (Web Share API พร้อม fallback เป็นดาวน์โหลด)

**Export อื่น**
- **JSON** — สำรอง/ย้ายเครื่อง และ **Import JSON** กลับเข้ามาได้
- **Excel (.xlsx)** ด้วย SheetJS — 1 รายงาน = 1 แถว สำหรับเอาไปทำสรุปรวมภายหลัง (คอลัมน์ตามฟิลด์ทั้งหมด + คะแนน)

---

## 9. แนวทางดีไซน์

- โทนสีมืออาชีพแบบสถาบันการเงิน: น้ำเงินเข้ม `#1B3A5F` เป็นสีหลัก, ฟ้าอ่อน `#DCE9F5` เป็นพื้นเน้น (ตามสีในฟอร์มเดิม), เทาอ่อนเป็นพื้นหลัง
- คอนทราสต์สูง อ่านกลางแดดได้ (ตัวอักษรเนื้อหา ≥ 16px, ป้ายชื่อฟิลด์ ≥ 14px ตัวหนา)
- ไม่ใช้ modal ซ้อน modal, ไม่ใช้ hover เป็นกลไกหลัก
- มี dark mode ก็ได้แต่ไม่จำเป็นใน v1
- แสดงสถานะออฟไลน์เป็นแถบเล็กๆ ("ทำงานแบบออฟไลน์ — ข้อมูลถูกบันทึกในเครื่อง")

---

## 10. เกณฑ์ตรวจรับ (ให้ตรวจทีละข้อก่อนส่งงาน)

- [ ] ปิด Wi-Fi แล้วยังสร้าง/กรอก/ถ่ายรูป/ออก PDF ได้ครบ
- [ ] ปิด Safari กลางคัน เปิดใหม่แล้วข้อมูลยังอยู่ครบ
- [ ] กรอกครบทุกฟิลด์ในข้อ 4 ได้ ไม่มีฟิลด์ไหนตกหล่นจากฟอร์มต้นฉบับ
- [ ] PDF ที่ออกมาอ่านง่าย ฟอนต์ไทยไม่เพี้ยน สระ/วรรณยุกต์ไม่หลุด
- [ ] ใช้งานได้จริงบน iPad ทั้งแนวตั้งและแนวนอน ไม่มี layout แตก
- [ ] เพิ่มรูป 10 รูปแล้วแอปยังไม่ช้าหรือค้าง
- [ ] ทุกปุ่มกดติดด้วยนิ้วโป้ง ไม่ต้องซูม

---

## 11. ขอบเขตที่ "ไม่ต้อง" ทำใน v1

- ระบบล็อกอิน / ผู้ใช้หลายคน
- ซิงก์ขึ้น server หรือฐานข้อมูลกลาง (ออกแบบเผื่อไว้ แต่ยังไม่ต้องทำ)
- Dashboard สรุปรวมหลายรายงาน
- ระบบอนุมัติออนไลน์ (workflow)
- แจ้งเตือน push

---

## 12. สิ่งที่ต้องส่งมอบ

1. โค้ดทั้งโปรเจกต์ พร้อม `README.md` ที่บอกวิธี build และวิธีติดตั้งลง iPad (Safari → แชร์ → เพิ่มไปยังหน้าจอโฮม)
2. ข้อมูลตัวอย่าง 1 ชุด (ใช้เคส GE TECH PACKING SOLUTION / GREEN PROUD TRADING) สำหรับกด "โหลดตัวอย่าง" เพื่อทดสอบ
3. ไฟล์ PDF ตัวอย่างที่ระบบสร้างจากข้อมูลชุดนั้น

**เริ่มจากทำ TypeScript types + โครงหน้าจอทั้งหมดให้เห็นภาพก่อน แล้วค่อยลงรายละเอียดทีละขั้น**
